# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — Python runtime dependencies for AlphaZ0
#
# For Docker (CPU-only, smaller image):
#   pip install torch==2.3.1+cpu --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt --ignore-requires-python
#
# For local with GPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# Neural network
torch>=2.0.0

# Board encoding / numeric ops
numpy>=1.24.0

# Web backend
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Request validation
pydantic>=2.0.0

# C++ extension build tools (needed at build time, not at runtime)
pybind11>=2.11.0
cmake>=3.20.0
setuptools>=68.0.0
